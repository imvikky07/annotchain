{
  "name": "annotation-contracts",
  "version": "1.0.0",
  "description": "Solidity smart contracts for the Blockchain Annotation Platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "fund:sepolia": "hardhat run scripts/fund.js --network sepolia",
    "verify": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.0.0"
  }
}
